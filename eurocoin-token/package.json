{
  "name": "eurocoin-token",
  "version": "1.0.0",
  "private": true,
  "description": "Phase 1 CLI proof-of-concept for the Solana Bonds platform (EuroCC stablecoin + BonoDeuda bond).",
  "type": "module",
  "scripts": {
    "dev": "npx tsx src/main.ts",
    "balance": "npx tsx src/balance.ts",
    "compra": "npx tsx src/compra.ts",
    "transfer": "npx tsx src/transfer.ts",
    "cupon": "npx tsx src/pago-cupon.ts",
    "test": "npm run dev && npm run balance && npm run compra && npm run transfer && npm run cupon && npm run balance",
    "build": "tsc"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.0",
    "@solana/web3.js": "^1.98.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
